
public interface IExportUtility {
//// create XML from status list and return as string
//static String toXML(List<ITweet> statusList);
//// saves the string (XML string) to the file specified
//static boolean saveToFile(String filename, String text);
}
